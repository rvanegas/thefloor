{
  "name": "@thefloor/core",
  "version": "1.0.0",
  "private": true,
  "description": "The floor mechanic, channel lifecycle, and recording rules. Depends on nothing — the app and the server both import it, so there is one implementation of the rules rather than two.",
  "main": "channel.ts",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "~6.0.3"
  }
}
